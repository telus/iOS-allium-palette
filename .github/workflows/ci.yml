name: Update Pod repo

on: [push]

jobs:
  build:
    runs-on: macOS-latest
    steps:
      - name: Checkout
      - uses: actions/checkout@v2

      - name: Update Cocoapod
        run: |
          python3 auto.py
        shell: sh